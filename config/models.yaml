# Deprecated: model targets are configured in the web UI (Models page).
# Data files:
#   config/model_catalog.yaml   — supported brands/models and scraper paths
#   config/models_selection.json — your active picks (up to 5)
